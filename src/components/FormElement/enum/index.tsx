export enum EnumFileType {
  Image = 'image',
  Video = 'video',
  Document = 'document',
}

export enum fileInputEnum {
  FileInput = 'fileInput',
  LinkFileInput = 'linkFileInput',
  LinkFileInputBlack = 'linkFileInputBlack',
  FileInputXLS = 'fileInputXLS',
  CommentFileInput = 'commentFileInput',
}

export enum MaterialType {
  Teaching = 'teaching',
  Practice = 'practice',
}
